<!doctype html>
<html lang="en">
   <head>
        <!-- Title block allowing child pages to override -->
        <title>{% block title %}{% endblock %} &ndash; Shropgeek (R)evolution 2015</title>
        {% include "html-head" %}
        <!-- Style block allowing child pages to inject their own styles -->
        {% block styles %}{% endblock %}
    </head>
    <body>
        <div class="container">
            {% include "header" %}
            <!-- Content block, child page inject content -->
            {% block content %}{% endblock %}
            {% include "footer.sponsors" %}
            {% include "footer" %}
        </div>
        {% include "html-footer" %}
        {% block scripts %}{% endblock %}
    </body>
</html>
